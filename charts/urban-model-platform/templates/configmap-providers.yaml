apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ump.fullname" . }}-providers
  labels:
    {{- include "ump.labels" . | nindent 4 }}
data:
  providers.yaml: {{- toYaml .Values.providersContent | indent 4 }}
