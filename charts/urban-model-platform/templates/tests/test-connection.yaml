apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ump-api-dev.fullname" . }}-test-connection"
  labels:
    {{- include "ump-api-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ump-api-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
