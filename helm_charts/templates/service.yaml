apiVersion: v1
kind: Service
metadata:
  name: {{ include "ump-api-dev.fullname" . }}
  labels:
    {{- include "ump-api-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ump-api-dev.selectorLabels" . | nindent 4 }}
